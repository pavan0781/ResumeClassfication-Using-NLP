# .streamlit/secrets.toml
# Add this file for Streamlit Cloud deployment

# Set to "true" to use FastAPI backend instead of local model
USE_API = "false"

# URL of your deployed FastAPI backend (Render/Railway)
# e.g. "https://resume-classifier-api.onrender.com"
API_URL = "http://localhost:8000"
